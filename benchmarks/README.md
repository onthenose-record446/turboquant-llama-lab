# Benchmarks

This directory is for publishable benchmark inputs and reports.

Planned contents:

- synthetic TurboQuant core runs
- llama.cpp profile comparisons
- memory-first profile reports
- speed-first profile reports

Recommended categories:

- `synthetic/`
- `llama_cpp/`
- `reports/`

The first benchmark entry point already exists:

- `turboquant-bench`
